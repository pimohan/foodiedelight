// Layout configuration for form items
export const formItemLayout = {
  // Label column configuration
  labelCol: {
    xs: { span: 24 }, // Full width on extra-small screens
    sm: { span: 6 }, // 6-column width on small screens and up
  },
  // Wrapper column configuration
  wrapperCol: {
    xs: { span: 24 }, // Full width on extra-small screens
    sm: { span: 14 }, // 14-column width on small screens and up
  },
};
